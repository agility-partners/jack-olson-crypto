name: crypto_pipeline
version: 1.0.0
config-version: 2

profile: crypto_pipeline

model-paths: ["models"]
macro-paths: ["macros"]

models:
  crypto_pipeline:
    silver:
      +schema: silver
      +materialized: view
    gold:
      +schema: gold
      +materialized: view
